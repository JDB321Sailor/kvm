# Local IP Address Configuration
# Set this to your local machine's IP address for deployment
LOCAL_IP=192.168.1.3

# Database Configuration
POSTGRES_PASSWORD=your_secure_postgres_password_here
DATABASE_URL="postgresql://jetkvm:your_secure_postgres_password_here@postgres:5432/jetkvm?schema=public"

# Authentication Provider Configuration
# Choose either 'google' or 'authentik' as your authentication provider
AUTH_PROVIDER=google

# Google OAuth Configuration (when AUTH_PROVIDER=google)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Authentik OIDC Configuration (when AUTH_PROVIDER=authentik)
# Replace 'your-authentik-domain.com' with your actual Authentik instance URL
AUTHENTIK_ISSUER=https://your-authentik-domain.com/application/o/jetkvm/
AUTHENTIK_CLIENT_ID=your_authentik_client_id_here
AUTHENTIK_CLIENT_SECRET=your_authentik_client_secret_here
# The authorization URL for your Authentik application
AUTHENTIK_AUTHORIZATION_URL=https://your-authentik-domain.com/application/o/authorize/
# The token URL for your Authentik application  
AUTHENTIK_TOKEN_URL=https://your-authentik-domain.com/application/o/token/
# The userinfo URL for your Authentik application
AUTHENTIK_USERINFO_URL=https://your-authentik-domain.com/application/o/userinfo/
# The JWKS URL for token verification
AUTHENTIK_JWKS_URL=https://your-authentik-domain.com/application/o/jetkvm/jwks/

# Hostnames for OAuth callbacks - Replace 192.168.1.3 with your LOCAL_IP
API_HOSTNAME=https://192.168.1.3/api
APP_HOSTNAME=https://192.168.1.3

# Cloudflare TURN (optional for local network)
# CLOUDFLARE_TURN_ID=
# CLOUDFLARE_TURN_TOKEN=

# Session Security (REQUIRED - generate with: openssl rand -base64 32)
COOKIE_SECRET="generate_with_openssl_rand_base64_32"

# File Storage (optional for basic setup)
# R2_ENDPOINT=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=
# R2_CDN_URL=

# CORS Origins - Replace 192.168.1.3 with your LOCAL_IP
CORS_ORIGINS=https://192.168.1.3,http://localhost:5173

# Reverse Proxy (use if setting up nginx)
REAL_IP_HEADER=X-Real-IP

# WebRTC ICE Servers
# ICE_SERVERS=stun:stun.l.google.com:19302,stun:stun1.l.google.com:19302